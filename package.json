{
  "name": "tcp-chat-websocket",
  "version": "1.0.0",
  "description": "Chat em tempo real (Socket.IO) — conversão do chat TCP/Tkinter para web",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  }
}
